The default images for Streameth are:

1. poster.png - used as a still image (16:9 ratio) within the player if streams aren't active. 
2. social.png - used as the default image (2:1) for open graph & social cards.
3. header.png - used as general impression of the event, city on the session detail pages.