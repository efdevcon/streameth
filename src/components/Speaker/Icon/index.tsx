import { Speaker } from 'types'

interface Props {
  speaker: Speaker
}

export default function SpeakerIcon({ speaker }: Props) {
  const image = () => {
    ;<div className="h-5 w-5 "></div>
  }

  return <div></div>
}
